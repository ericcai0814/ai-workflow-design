# Design: AI Coding Workflow Skill 架構設計

## 架構概覽

```
┌─────────────────────────────────────────────────────────────┐
│                     使用者介面                               │
│   「/ai-coding-workflow」或「建立一個 Button 元件」          │
└─────────────────────────────────────┬───────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────┐
│                   SKILL.md（統一入口）                       │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 索引邏輯：                                           │   │
│  │ - 分析需求 → references/01-planning/                │   │
│  │ - 建立元件 → references/02-development/frontend/    │   │
│  │ - API 設計 → references/02-development/backend/     │   │
│  │ - 程式碼審查 → references/03-review/                │   │
│  │ - 排查問題 → references/appendix/pitfalls/          │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────┬───────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────┐
│                   references/（知識庫）                      │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐          │
│  │01-planning/ │ │02-develop/  │ │03-review/   │  ...     │
│  │             │ │frontend/    │ │             │          │
│  │             │ │backend/     │ │             │          │
│  └──────┬──────┘ └──────┬──────┘ └──────┬──────┘          │
└─────────┼───────────────┼───────────────┼───────────────────┘
          │               │               │
          ▼               ▼               ▼
┌─────────────────────────────────────────────────────────────┐
│                   scripts/（輔助腳本）                       │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐          │
│  │create-      │ │create-      │ │run-         │          │
│  │component.sh │ │dbml.sh      │ │tests.sh     │          │
│  └─────────────┘ └─────────────┘ └─────────────┘          │
└─────────────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────┐
│                   產出（70% MVP）                            │
│  「MVP 已完成，請檢查後告訴我需要調整的地方」                  │
└─────────────────────────────────────────────────────────────┘
```

## 目錄結構

```
.claude/skills/ai-coding-workflow/
├── SKILL.md                        # ⭐ 主要 skill 文件
├── README.md                       # 使用指南
├── QUICKSTART.md                   # 快速開始
│
├── references/                     # 📚 知識庫
│   ├── 01-planning/
│   │   ├── overview.md
│   │   ├── task-decomposition.md
│   │   ├── tech-stack-selection.md
│   │   └── prompts/
│   │       ├── analyze-requirements.md
│   │       └── create-plan.md
│   │
│   ├── 02-development/
│   │   ├── frontend/
│   │   │   ├── overview.md
│   │   │   ├── design-system.md
│   │   │   ├── component-development.md
│   │   │   ├── state-management.md
│   │   │   ├── routing.md
│   │   │   └── prompts/
│   │   │       ├── setup-design-system.md
│   │   │       ├── setup-token-system.md
│   │   │       ├── create-component.md
│   │   │       ├── setup-routing.md
│   │   │       └── setup-state.md
│   │   │
│   │   ├── backend/
│   │   │   ├── overview.md
│   │   │   ├── api-design.md
│   │   │   ├── database.md
│   │   │   ├── authentication.md
│   │   │   └── prompts/
│   │   │       ├── design-api.md
│   │   │       ├── create-model.md
│   │   │       └── setup-auth.md
│   │   │
│   │   └── shared/
│   │       ├── validation-framework.md
│   │       ├── integration.md
│   │       ├── testing.md
│   │       └── prompts/
│   │           ├── feature-implementation.md
│   │           ├── bug-fixing.md
│   │           └── integration-test.md
│   │
│   ├── 03-review/
│   │   ├── overview.md
│   │   ├── code-review-checklist.md
│   │   └── prompts/
│   │       └── review-code.md
│   │
│   └── appendix/
│       ├── pitfalls/
│       │   ├── index.md
│       │   └── case-*.md
│       ├── success-cases/
│       │   ├── index.md
│       │   └── case-*.md
│       ├── tech-stack-examples/
│       │   ├── vue3-vuetify/
│       │   └── react-nextjs/
│       └── prompt-cheatsheet.md
│
├── scripts/                        # 輔助腳本
│   ├── create-component.sh
│   ├── create-dbml.sh
│   └── run-tests.sh
│
├── templates/                      # 模板
│   ├── task-plan.md
│   ├── feature-spec.md
│   ├── component-spec.md
│   ├── api-spec.md
│   └── database-schema.md
│
└── assets/                         # 資源
```

## SKILL.md 設計

### Frontmatter

```yaml
---
name: ai-coding-workflow
description: 團隊標準化開發工作流程。使用時機：開始新專案、建立設計系統、開發元件/API、排查問題。根據任務類型自動導向對應的參考文件，快速產出 70% MVP。
---
```

### 內容結構

```markdown
# AI Coding Workflow

## 概述

這個 skill 提供團隊標準化的 AI 輔助開發工作流程...

## 使用時機

### 規劃階段
**關鍵字**：分析需求、建立計畫、專案規劃、技術選型
**讀取**：`references/01-planning/`

### 前端開發
**關鍵字**：設計系統、建立元件、前端、UI、樣式
**讀取**：`references/02-development/frontend/`

### 後端開發
**關鍵字**：API 設計、資料庫、後端、認證
**讀取**：`references/02-development/backend/`

### 程式碼審查
**關鍵字**：程式碼審查、review、PR
**讀取**：`references/03-review/`

### 問題排查
**關鍵字**：問題、錯誤、bug、不 work
**讀取**：`references/appendix/pitfalls/`

## 執行流程

1. 根據任務關鍵字判斷類型
2. 讀取對應的 references 文件
3. 執行任務（可使用 scripts/ 腳本）
4. 產出 70% MVP
5. 提示人工迭代

## 70% MVP 標準

...

## 文件索引

...

## 快速參考

...
```

## 腳本設計（scripts/）

### create-component.sh

```bash
#!/bin/bash
# 建立元件檔案結構

COMPONENT_NAME=$1
COMPONENT_DIR="src/components/${COMPONENT_NAME}"

mkdir -p "${COMPONENT_DIR}"

# 建立檔案
touch "${COMPONENT_DIR}/${COMPONENT_NAME}.tsx"
touch "${COMPONENT_DIR}/${COMPONENT_NAME}.test.tsx"
touch "${COMPONENT_DIR}/index.ts"

echo "✅ 已建立元件結構：${COMPONENT_DIR}"
```

### create-dbml.sh

```bash
#!/bin/bash
# 建立 DBML 資料庫定義檔

TABLE_NAME=$1
DBML_FILE="database/${TABLE_NAME}.dbml"

mkdir -p database

cat > "${DBML_FILE}" << EOF
Table ${TABLE_NAME} {
  id uuid [pk]
  created_at timestamp [default: 'now()']
  updated_at timestamp
}
EOF

echo "✅ 已建立 DBML：${DBML_FILE}"
```

## 70% MVP 定義

### 通用標準

**驗收標準**：樣式正常、功能正常

| 項目 | MVP 包含 | 人工迭代 |
|------|----------|----------|
| 檔案結構 | ✅ 正確位置、正確命名 | - |
| 基本功能 | ✅ 核心功能可運行 | 功能調整、功能疊加 |
| 樣式 | ✅ 套用 Design Token、樣式正常 | 細節微調 |
| 型別定義 | ✅ 主要 props/types | 完整型別 |
| 邊界處理 | ⏸️ 基本處理 | 完整邊界處理 |
| 測試 | ⏸️ 基本 render 測試 | 完整測試案例 |

### 場景範例：建立 Button 元件

**MVP 產出**：

```tsx
interface ButtonProps {
  children: React.ReactNode;
  variant?: 'primary' | 'secondary';
  onClick?: () => void;
}

export function Button({
  children,
  variant = 'primary',
  onClick
}: ButtonProps) {
  return (
    <button
      className={`btn btn-${variant}`}
      onClick={onClick}
      data-testid="button"
    >
      {children}
    </button>
  );
}
```

**人工迭代**：
- disabled 狀態
- loading 狀態
- size 變體
- icon 支援
- 完整 a11y

## 遷移策略

### 從現有 ai-coding-workflow 遷移

1. 建立新目錄結構
2. 複製文件到 `references/`
3. 翻譯為繁體中文
4. 建立 SKILL.md
5. 測試調用
6. 移除舊目錄

### 文件對應

| 舊位置 | 新位置 |
|--------|--------|
| `ai-coding-workflow/01-planning/` | `references/01-planning/` |
| `ai-coding-workflow/02-development/` | `references/02-development/` |
| `ai-coding-workflow/03-review/` | `references/03-review/` |
| `ai-coding-workflow/appendix/` | `references/appendix/` |
| `ai-coding-workflow/templates/` | `templates/` |

## 分發方式

```bash
# 使用 degit 複製到其他專案
cd /path/to/your-project
degit ericcai0814/ai-workflow-design/.claude/skills/ai-coding-workflow .claude/skills/ai-coding-workflow
```
